var Lists = Lists || {};

(function(lists) {
  lists.page = {
    
    init : function() {
    },

  //Add List

  //Remove List
  //Add Item (item update api, no id)
  //Remove Item (item update api, hidden field =true)
  //Reorder Item (item update api, order field updated, will be a list of items)
  //Update Item (item update api, fields updated)

  };
})(Preferences);

$(document).ready(function() {
  Preferences.page.init();
});